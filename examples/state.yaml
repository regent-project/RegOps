Attributes:
  - Name: web server is installed
    Privilege: !WithSudo
    Timeout: 30
    Detail: !Apt
      Package: apache2
      State: Present
  - Name: web service is always running
    Privilege: !WithSudo
    Timeout: 5
    Detail: !Service
      Name: apache2
      State: Started
      Enabled: true