Attributes:
  - Name: web server is installed
    Privilege: !WithSudo
    Timeout: 10
    Detail: !Apt
      Package: apache2
      State: Present
  - Name: web server is always running
    Privilege: !WithSudo
    Timeout: 1
    Detail: !Service
      Name: apache2
      State: Started
      Enabled: true